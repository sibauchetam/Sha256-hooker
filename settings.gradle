pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Add the Xposed repository to resolve the API dependency
        maven { url 'https://api.xposed.info/' }
    }
}
rootProject.name = "XposedDigest"
include ':app'